export enum CMDS {
   INLINE_FOLD_TOGGLE = "inlineFold.toggle",
   INLINE_FOLD_DEFAULT = "inlineFold.enabled",
}
export enum CONFIGS {
   ENABLED = "enabled",
   IDENTIFIER = "inlineFold",
   REGEX = "regex",
   REGEX_FLAGS = "regexFlags",
   REGEX_GROUP = "regexGroup",
   MASK_CHAR = "maskChar",
   MASK_COLOR = "maskColor",
   UNFOLDED_OPACITY = "unfoldedOpacity",
   AFTER = "after",
   SUPPORTED_LANGUAGES = "supportedLanguages",
   UNFOLDED_ON_LINE_SELECT = "unfoldOnLineSelect",
}
